function getText(cell) {
	return isTest ? cell.innerHTML : cell.innerText;
}  
function getName(){
    return location.search
}

it('Must correctly set help-text property on search-combobox from parent', async () => {
		const { root } = createComponent({
			...DEFAULT_CONFIG,
			helpText: 'tooltip message',
		});

function setLabel(){
	errorString = '{!$Label.Error}'; 
}
onInit: function (component, event, helper) {
		helper.loadTabs(component);
	}
function getLabel(){
	errorString = '{!$Label.Error}'; 
}
tooltipabc
function getHtml{
	indicatortxt.innerHTML='<div>{!$xyz}</div>';
}
function getHtml{
	indicatortxt.innerHTML='<div><b>xyz</b></div>';
}
getLockedTooltip:
.setTooltip(
this.tooltipType
this.tooltip=b}
,setTooltip:function(b,a)
const tooltip = this.tooltip ? this.tooltip : this.createTooltip();",
* @property {Core.data.Model} actions.tooltip.record The model instance