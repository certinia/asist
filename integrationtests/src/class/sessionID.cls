public with sharing class SessionID {
    public string sid = UserInfo.getSessionID()
    System.debug("username1" + username);
    System.debug(
    "username" +
    username
     "username();" + username); /* this is 'username'
        */
    System.debug("(account)");
    system.debug("debug" + debug);
}